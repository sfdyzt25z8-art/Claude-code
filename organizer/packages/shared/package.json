{
  "name": "@organizer/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types, theme tokens, constants and i18n strings used by the Organizer backend, web and mobile apps",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
